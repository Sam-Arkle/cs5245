# results = [r.text.split('<p>')[0].strip() for r in all_results]

# get_views()
# match_views = {}
# for element in pages:
#     match_views[element] = str(get_views(element))

# for key in match_views:
#     print(key, match_views[key])

# with open('comments.jl', 'a') as f:
#     json_dict = json.dump(match_views, f, indent=4)

# for key in match_views:
#     match_views[key] = match_views[key]
#     print(key, match_views[key])

# example = Event('https://roysviewfrom.com/2022/10/07/pre-math-view-from-stoke/')
# print(example.url, example.date, example.match)

# file.write(MyEncoder().encode(page_object) + '\n')

# json.dump(page_object.__dict__, file,  indent=4)
